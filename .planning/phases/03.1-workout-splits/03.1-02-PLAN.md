---
phase: 03.1-workout-splits
plan: 02
type: execute
wave: 2
depends_on: ["03.1-01"]
files_modified:
  - src/navigation/types.ts
  - src/navigation/RootNavigator.tsx
  - src/screens/workout/SplitListScreen.tsx
  - src/components/workout/SplitCard.tsx
autonomous: true

must_haves:
  truths:
    - "Train tab shows list of saved splits"
    - "Empty state shows when no splits exist"
    - "Each split card shows name and exercise count"
    - "Create button navigates to SplitCreate screen"
  artifacts:
    - path: "src/screens/workout/SplitListScreen.tsx"
      provides: "Train tab split list"
      min_lines: 80
    - path: "src/components/workout/SplitCard.tsx"
      provides: "Split list item component"
      min_lines: 40
    - path: "src/navigation/types.ts"
      provides: "Navigation params for split screens"
      contains: "SplitCreate"
  key_links:
    - from: "src/screens/workout/SplitListScreen.tsx"
      to: "useSplits"
      via: "hook import"
      pattern: "useSplits\\(\\)"
    - from: "SplitCard"
      to: "navigation"
      via: "onPress prop"
      pattern: "onPress"
---

<objective>
Build the SplitListScreen to replace the LOCKED placeholder on the Train tab, showing saved splits and a create button.

Purpose: Users need to see their splits and access the create flow. This is the entry point to the splits feature.
Output: Working Train tab with FlatList of splits, empty state, and navigation to create screen.
</objective>

<execution_context>
@C:\Users\alist\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\alist\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03.1-workout-splits/03.1-RESEARCH.md
@.planning/phases/03.1-workout-splits/03.1-01-SUMMARY.md

@src/screens/workout/WorkoutListScreen.tsx
@src/components/workout/ExerciseCard.tsx
@src/hooks/useSplits.ts
@src/navigation/types.ts
@src/navigation/RootNavigator.tsx
@src/constants/colors.ts
@src/constants/fonts.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update navigation types and RootNavigator</name>
  <files>src/navigation/types.ts, src/navigation/RootNavigator.tsx</files>
  <action>
1. In src/navigation/types.ts, add to RootStackParamList:
   - SplitCreate: undefined
   - SplitDetail: { splitId: string }

2. In src/navigation/RootNavigator.tsx:
   - Import SplitCreateScreen and SplitDetailScreen (will be created in later plans)
   - Add placeholder Stack.Screen entries for them:
     ```tsx
     <Stack.Screen name="SplitCreate" component={SplitCreateScreen} />
     <Stack.Screen name="SplitDetail" component={SplitDetailScreen} />
     ```
   - For now, create minimal placeholder components inline or in separate files to prevent import errors:
     ```tsx
     // Temporary until Plan 03 creates real screens
     function SplitCreateScreen() {
       return <View style={{ flex: 1, backgroundColor: '#0A0A0F' }}><Text>Create</Text></View>;
     }
     function SplitDetailScreen() {
       return <View style={{ flex: 1, backgroundColor: '#0A0A0F' }}><Text>Detail</Text></View>;
     }
     ```
   - Import View and Text from react-native for placeholders
  </action>
  <verify>
`npx tsc --noEmit 2>&1 | grep -v "node_modules" | head -20` shows no navigation type errors
  </verify>
  <done>
Navigation types include SplitCreate and SplitDetail routes. RootNavigator has placeholder screens.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create SplitCard component</name>
  <files>src/components/workout/SplitCard.tsx</files>
  <action>
Create src/components/workout/SplitCard.tsx following ExerciseCard patterns:

Props interface:
```typescript
interface SplitCardProps {
  split: WorkoutSplit;
  onPress: () => void;
}
```

Component structure:
- Pressable container with cyberpunk styling (bg.card background, cold border, 4px border-radius)
- Row layout with:
  - Left side: Split name (fonts.label, 16px, text.primary) and exercise count below (fonts.monoLight, 10px, text.muted, e.g., "4 EXERCISES")
  - Right side: Chevron or arrow indicator ("> " in fonts.mono, ember[500])
- Haptics.impactAsync on press (Light feedback)

Styles following project patterns:
- Use StyleSheet.create at bottom
- Import colors from @/constants/colors
- Import fonts from @/constants/fonts
- Container padding: 12px horizontal, 14px vertical
- marginBottom: 8 for list spacing
  </action>
  <verify>
`npx tsc --noEmit 2>&1 | grep SplitCard` shows no errors
  </verify>
  <done>
SplitCard component renders split name, exercise count, and handles press with haptic feedback.
  </done>
</task>

<task type="auto">
  <name>Task 3: Replace WorkoutListScreen with SplitListScreen</name>
  <files>src/screens/workout/SplitListScreen.tsx</files>
  <action>
Create src/screens/workout/SplitListScreen.tsx (new file, rename happens in MainTabs):

1. Imports:
   - React, useState, useCallback from 'react'
   - StyleSheet, Text, View, FlatList, Pressable from 'react-native'
   - useFocusEffect from '@react-navigation/native'
   - Haptics from 'expo-haptics'
   - useNavigation, NavigationProp from @react-navigation/native
   - colors, fonts from @/constants
   - useSplits from @/hooks/useSplits
   - SplitCard from @/components/workout/SplitCard
   - RootStackParamList from @/navigation/types

2. Component structure:
   - Header bar (44px height, same style as ExerciseSelectScreen):
     - "SPLITS" title left (fonts.display, 18px, text.primary, letterSpacing: 2)
     - "+ NEW" button right (fonts.mono, 12px, ember[500], with border)

   - FlatList of splits:
     - Data from useSplits().splits
     - renderItem: SplitCard with onPress -> navigation.navigate('SplitDetail', { splitId })
     - keyExtractor: split.id
     - contentContainerStyle with padding

   - Empty state (when splits.length === 0 and !isLoading):
     - Centered view
     - "NO SPLITS YET" text (fonts.monoLight, 12px, text.muted)
     - "Create your first workout split" subtext
     - "CREATE SPLIT" button (ember border, ember text)

3. Refresh on focus using useFocusEffect:
   ```tsx
   useFocusEffect(
     useCallback(() => {
       refetch();
     }, [refetch])
   );
   ```

4. Navigation handler:
   - handleCreatePress -> Haptics + navigation.navigate('SplitCreate')
   - handleSplitPress(splitId) -> navigation.navigate('SplitDetail', { splitId })

5. Update MainTabs.tsx to import SplitListScreen instead of WorkoutListScreen for the Train tab.
  </action>
  <verify>
1. Launch app: `npx expo start --android`
2. Navigate to Train tab
3. Verify "SPLITS" header and "+ NEW" button appear
4. Verify empty state shows "NO SPLITS YET"
5. Tap "+ NEW" navigates to placeholder SplitCreate screen
  </verify>
  <done>
Train tab shows SplitListScreen with empty state, header, and navigation to create screen.
  </done>
</task>

</tasks>

<verification>
1. App launches and Train tab no longer shows "LOCKED"
2. Train tab shows "SPLITS" header with "+ NEW" button
3. Empty state renders when no splits exist
4. Tapping "+ NEW" navigates to SplitCreate placeholder
5. No TypeScript errors
</verification>

<success_criteria>
- Train tab displays SplitListScreen instead of WorkoutListScreen
- Header shows "SPLITS" title and "+ NEW" button with correct fonts/colors
- Empty state visible when no splits exist
- Navigation to SplitCreate works (shows placeholder)
- SplitCard component ready for rendering split items
</success_criteria>

<output>
After completion, create `.planning/phases/03.1-workout-splits/03.1-02-SUMMARY.md`
</output>

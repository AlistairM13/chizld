---
phase: 03.1-workout-splits
plan: 03
type: execute
wave: 2
depends_on: ["03.1-01"]
files_modified:
  - src/screens/workout/SplitCreateScreen.tsx
  - src/components/workout/ZoneFilterBar.tsx
  - src/components/workout/SplitExerciseRow.tsx
autonomous: true

must_haves:
  truths:
    - "User can name the split"
    - "User can filter exercises by zone using horizontal pills"
    - "User can select exercises from ANY zone (multi-zone)"
    - "Each selected exercise shows configurable sets/reps"
    - "Save button creates split and navigates back"
  artifacts:
    - path: "src/screens/workout/SplitCreateScreen.tsx"
      provides: "Split creation flow"
      min_lines: 150
    - path: "src/components/workout/ZoneFilterBar.tsx"
      provides: "Horizontal zone filter pills"
      min_lines: 40
    - path: "src/components/workout/SplitExerciseRow.tsx"
      provides: "Exercise row with sets/reps inputs"
      min_lines: 60
  key_links:
    - from: "src/screens/workout/SplitCreateScreen.tsx"
      to: "useSplits.createSplit"
      via: "hook call"
      pattern: "createSplit\\("
    - from: "ZoneFilterBar"
      to: "SplitCreateScreen"
      via: "onSelect callback"
      pattern: "onSelect"
---

<objective>
Build the SplitCreateScreen with multi-zone exercise selection and per-exercise sets/reps configuration.

Purpose: Users need to create splits by selecting exercises across zones and configuring defaults. This is the core UX of the splits feature.
Output: Complete create flow with zone filter, exercise selection, sets/reps config, and save functionality.
</objective>

<execution_context>
@C:\Users\alist\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\alist\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03.1-workout-splits/03.1-RESEARCH.md
@.planning/phases/03.1-workout-splits/03.1-01-SUMMARY.md

@src/screens/workout/ExerciseSelectScreen.tsx
@src/components/workout/RepsInput.tsx
@src/components/workout/ExerciseCard.tsx
@src/hooks/useSplits.ts
@src/hooks/useExercises.ts
@src/constants/zones.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create ZoneFilterBar component</name>
  <files>src/components/workout/ZoneFilterBar.tsx</files>
  <action>
Create src/components/workout/ZoneFilterBar.tsx:

Props interface:
```typescript
interface ZoneFilterBarProps {
  selectedZone: string | null;  // null = ALL
  onSelect: (zoneId: string | null) => void;
}
```

Component:
1. Import zones from @/constants/zones
2. Create array with "ALL" option: `[{ id: null, name: 'ALL' }, ...zones]`
3. Horizontal ScrollView with showsHorizontalScrollIndicator={false}
4. Map over zones to render Pressable pills:
   - Each pill: paddingHorizontal: 14, paddingVertical: 6, marginRight: 8, borderRadius: 4
   - Unselected: bg.card background, cold border, text.muted text
   - Selected: ember[500] border, rgba(255,140,26,0.1) background, ember[500] text
   - Text: fonts.monoLight, 11px, letterSpacing: 1, uppercase
5. Haptics.impactAsync(Light) on press
6. onPress calls onSelect(zone.id)

Container style:
- paddingHorizontal: 12
- paddingVertical: 8
- borderBottomWidth: 1
- borderBottomColor: colors.zone.cold
  </action>
  <verify>
`npx tsc --noEmit 2>&1 | grep ZoneFilterBar` shows no errors
  </verify>
  <done>
ZoneFilterBar renders horizontal scrollable zone pills with ALL option and selection state.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create SplitExerciseRow component</name>
  <files>src/components/workout/SplitExerciseRow.tsx</files>
  <action>
Create src/components/workout/SplitExerciseRow.tsx:

Props interface:
```typescript
interface SplitExerciseRowProps {
  exerciseName: string;
  zoneName: string;
  sets: number;
  reps: number;
  onSetsChange: (value: number) => void;
  onRepsChange: (value: number) => void;
  onRemove: () => void;
}
```

Component layout (row with flex):
1. Left section (flex: 1):
   - Exercise name (fonts.label, 14px, text.primary)
   - Zone name below (fonts.monoLight, 10px, text.muted, uppercase)

2. Middle section (sets/reps inputs):
   - Two mini steppers side by side
   - Each stepper: [-] [value] [+] inline
   - Stepper buttons: 24x24, cold border, fonts.mono, ember text
   - Value display: 20px wide, centered, fonts.mono, 14px
   - Label above each: "SETS" / "REPS" (fonts.monoLight, 8px, text.muted)
   - Sets range: 1-10, Reps range: 1-30

3. Right section:
   - Remove button "X" (fonts.mono, ember[500], 20x20, with haptics)

Container style:
- paddingHorizontal: 12
- paddingVertical: 10
- backgroundColor: bg.card
- borderWidth: 1
- borderColor: cold
- borderRadius: 4
- marginBottom: 8
- flexDirection: 'row'
- alignItems: 'center'

Stepper logic (reuse pattern from RepsInput):
- Haptics.impactAsync on button press
- Clamp values within range
  </action>
  <verify>
`npx tsc --noEmit 2>&1 | grep SplitExerciseRow` shows no errors
  </verify>
  <done>
SplitExerciseRow displays exercise with editable sets/reps steppers and remove button.
  </done>
</task>

<task type="auto">
  <name>Task 3: Build SplitCreateScreen</name>
  <files>src/screens/workout/SplitCreateScreen.tsx</files>
  <action>
Create src/screens/workout/SplitCreateScreen.tsx (replace the placeholder in RootNavigator):

State:
```typescript
const [splitName, setSplitName] = useState('');
const [selectedZone, setSelectedZone] = useState<string | null>(null);
const [selectedExercises, setSelectedExercises] = useState<Map<string, { sets: number; reps: number }>>(new Map());
// Map key = exerciseId, value = { sets, reps } config
```

IMPORTANT: Use Map not Set to store per-exercise config. selectedExercises persists across zone filter changes.

Data fetching:
- Create useAllExercises hook OR query exercises without zone filter:
  - Simplest: modify useExercises to accept optional zoneId (null = all zones)
  - OR inline query: db.getAllAsync('SELECT * FROM exercises ORDER BY primary_zone, name')
- Filter displayed exercises by selectedZone (null shows all)

Layout (top to bottom):
1. Header (44px):
   - Back button "<" (fonts.mono, ember, onPress -> navigation.goBack)
   - "NEW SPLIT" title (fonts.display, 18px)
   - Spacer (flex: 1)

2. Split name input section:
   - Label "SPLIT NAME" (fonts.monoLight, 10px, text.muted)
   - TextInput (bg.card, cold border, fonts.label, 14px)
   - Placeholder: "e.g., Push Day, Pull Day..."

3. ZoneFilterBar component

4. Exercise selection list (FlatList):
   - Show exercises filtered by selectedZone
   - Render ExerciseCard for each (reuse existing component)
   - isSelected = selectedExercises.has(exercise.id)
   - onToggle: toggle in/out of Map with default { sets: 3, reps: 10 }

5. Selected exercises section (below or in separate scrollable area):
   - Header: "SELECTED ({count})" if count > 0
   - FlatList/ScrollView of SplitExerciseRow for each selected exercise
   - onSetsChange/onRepsChange update the Map entry
   - onRemove removes from Map

6. Bottom bar (fixed):
   - "SAVE SPLIT" button (full width, ember background, disabled if name empty or no exercises)
   - onPress: createSplit(name, exercises array), then navigation.goBack()

Key behaviors:
- Zone filter only affects DISPLAY, not selection state
- Switching zones keeps all selections intact
- Selected exercises always visible in bottom section regardless of filter
- Save converts Map to array: Array.from(selectedExercises.entries()).map(([id, cfg]) => ({ exerciseId: id, ...cfg }))

Update RootNavigator.tsx:
- Remove the inline placeholder SplitCreateScreen
- Import the real SplitCreateScreen from @/screens/workout/SplitCreateScreen
  </action>
  <verify>
1. Launch app, go to Train tab, tap "+ NEW"
2. Enter split name "Push Day"
3. Tap zone pills to filter exercises
4. Select exercises from multiple zones
5. Verify selections persist when changing zone filter
6. Adjust sets/reps on selected exercises
7. Tap "SAVE SPLIT"
8. Verify navigation returns to split list
  </verify>
  <done>
SplitCreateScreen allows naming split, selecting exercises across zones, configuring sets/reps, and saving.
  </done>
</task>

</tasks>

<verification>
1. ZoneFilterBar renders all zones plus ALL option
2. Tapping zone pill filters exercise list
3. Selected exercises persist across zone filter changes (critical!)
4. SplitExerciseRow shows sets/reps steppers that work
5. Save button creates split and returns to list
6. New split appears in SplitListScreen
</verification>

<success_criteria>
- User can name a split via text input
- Zone filter pills work and show ALL + 8 zones
- Exercises from ANY zone can be selected (not zone-locked)
- Selected exercises show in a list with editable sets (1-10) and reps (1-30)
- Save creates split in database and navigates back
- Split appears in list with correct exercise count
</success_criteria>

<output>
After completion, create `.planning/phases/03.1-workout-splits/03.1-03-SUMMARY.md`
</output>

---
phase: 04-workout-module
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/navigation/types.ts
  - src/navigation/RootNavigator.tsx
  - src/screens/character/CharacterScreen.tsx
  - src/hooks/useExercises.ts
  - src/screens/workout/ExerciseSelectScreen.tsx
  - src/components/workout/ExerciseCard.tsx
  - src/components/workout/ExerciseBottomBar.tsx
autonomous: true
user_setup: []

must_haves:
  truths:
    - "Tapping TRAIN from stat card navigates to ExerciseSelectScreen"
    - "ExerciseSelectScreen shows only exercises for the selected zone"
    - "Exercises are searchable by name"
    - "User can select multiple exercises with visual confirmation"
    - "Bottom bar shows selection count and Start Workout button"
  artifacts:
    - path: "src/navigation/types.ts"
      provides: "RootStackParamList with ExerciseSelect, WorkoutSession, SessionSummary"
      contains: "ExerciseSelect"
    - path: "src/screens/workout/ExerciseSelectScreen.tsx"
      provides: "Zone-filtered exercise selection screen"
      min_lines: 100
    - path: "src/hooks/useExercises.ts"
      provides: "Exercise query hook with zone filtering"
      exports: ["useExercises"]
    - path: "src/components/workout/ExerciseCard.tsx"
      provides: "Exercise list item with selection state"
      min_lines: 40
  key_links:
    - from: "src/screens/character/CharacterScreen.tsx"
      to: "ExerciseSelectScreen"
      via: "navigation.navigate on TRAIN press"
      pattern: "navigate.*ExerciseSelect"
    - from: "src/screens/workout/ExerciseSelectScreen.tsx"
      to: "src/hooks/useExercises.ts"
      via: "useExercises hook call"
      pattern: "useExercises"
---

<objective>
Wire the TRAIN button to navigate to ExerciseSelectScreen, create the exercise selection flow with zone filtering and multi-select.

Purpose: Enable users to begin a workout by selecting exercises for their chosen zone.
Output: Navigation wired, ExerciseSelectScreen functional with search and multi-select.
</objective>

<execution_context>
@C:\Users\alist\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\alist\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-workout-module/04-CONTEXT.md
@.planning/phases/04-workout-module/04-RESEARCH.md
@src/navigation/types.ts
@src/navigation/RootNavigator.tsx
@src/screens/character/CharacterScreen.tsx
@src/components/character/StatCard.tsx
@src/db/database.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Navigation Setup and Types</name>
  <files>
    src/navigation/types.ts
    src/navigation/RootNavigator.tsx
    src/screens/character/CharacterScreen.tsx
  </files>
  <action>
Update RootStackParamList in types.ts with workout stack params:
- ExerciseSelect: { zoneId: string }
- WorkoutSession: { sessionId: string; exercises: string[] }
- SessionSummary: { sessionId: string }

Update RootNavigator.tsx to add Stack.Screen entries for ExerciseSelect, WorkoutSession, and SessionSummary (create placeholder screens initially that just render "Coming" text - they'll be implemented in this and subsequent plans).

In CharacterScreen.tsx, wire the onTrain callback passed to StatCard to call navigation.navigate('ExerciseSelect', { zoneId: selectedZone }). The selectedZone is already tracked in state.

Use useNavigation hook with proper typing: useNavigation<NativeStackNavigationProp<RootStackParamList>>().
  </action>
  <verify>
App builds without errors. Tapping TRAIN button in detail mode shows the placeholder ExerciseSelect screen.
  </verify>
  <done>
TRAIN button navigates to ExerciseSelect screen with zoneId param.
  </done>
</task>

<task type="auto">
  <name>Task 2: useExercises Hook and ExerciseCard Component</name>
  <files>
    src/hooks/useExercises.ts
    src/components/workout/ExerciseCard.tsx
  </files>
  <action>
Create useExercises hook (src/hooks/useExercises.ts):
- Accept zoneId parameter and optional searchQuery
- Query exercises table: SELECT * FROM exercises WHERE primary_zone = ? AND (name LIKE '%' || ? || '%' OR ? = '')
- Use useSQLiteContext() following established pattern from useZoneStats
- Return { exercises: Exercise[], isLoading: boolean }
- Exercise type: { id: string; name: string; primaryZone: string; equipment: string | null }

Create ExerciseCard component (src/components/workout/ExerciseCard.tsx):
- Props: exercise: Exercise, isSelected: boolean, onToggle: () => void
- Display exercise name (Barlow Condensed, 16px, white)
- Display equipment if present (JetBrains Mono, 10px, text.muted)
- Selected state: ember-orange left border (2px), subtle ember background tint
- Unselected state: grey border, card background
- Pressable wrapper, call onToggle on press
- Haptic feedback on toggle: Haptics.impactAsync(ImpactFeedbackStyle.Light)
- Cyberpunk aesthetic: sharp corners (borderRadius: 4), card background
  </action>
  <verify>
Files created without type errors. Hook returns exercises array when called with valid zoneId.
  </verify>
  <done>
useExercises fetches zone-filtered exercises. ExerciseCard renders with selection state.
  </done>
</task>

<task type="auto">
  <name>Task 3: ExerciseSelectScreen with Search and Multi-Select</name>
  <files>
    src/screens/workout/ExerciseSelectScreen.tsx
    src/components/workout/ExerciseBottomBar.tsx
  </files>
  <action>
Create ExerciseBottomBar component (src/components/workout/ExerciseBottomBar.tsx):
- Props: count: number, onStart: () => void, disabled: boolean
- Display "{count} EXERCISES SELECTED" (JetBrains Mono, 12px)
- START WORKOUT button (ember-orange background, disabled when count=0)
- Fixed at bottom, 60px height, card background, top border ember

Create ExerciseSelectScreen (src/screens/workout/ExerciseSelectScreen.tsx):
- Read zoneId from route params
- Use useState for selectedExercises: Set<string>
- Use useState for searchQuery: string
- Use useExercises(zoneId, searchQuery) to fetch exercises
- Render search input at top:
  - Placeholder: "SEARCH EXERCISES..."
  - JetBrains Mono font, 14px
  - Card background, ember border on focus
  - Filter exercises as user types
- Render FlatList of ExerciseCard components
- Toggle selection on card press (add/remove from Set)
- Render ExerciseBottomBar at bottom with count and Start handler
- On Start: navigation.navigate('WorkoutSession', { exercises: Array.from(selectedExercises), sessionId: generateId() })
- generateId: Date.now().toString(36) + Math.random().toString(36).substr(2)
- Header: zone name in caps (Chakra Petch) with back arrow

Styling:
- Full screen height minus bottom bar
- Dark background (bg.primary)
- Ember accent color throughout
  </action>
  <verify>
Navigate to ExerciseSelectScreen, exercises appear filtered by zone. Search filters list. Selecting exercises updates bottom bar count. Tapping Start navigates (to placeholder WorkoutSession).
  </verify>
  <done>
ExerciseSelectScreen shows zone exercises, supports search and multi-select, bottom bar shows count and Start button.
  </done>
</task>

</tasks>

<verification>
1. App builds: `npm start` runs without errors
2. Navigation flow: TRAIN -> ExerciseSelectScreen works
3. Zone filtering: Only exercises for selected zone appear
4. Search: Typing filters the exercise list
5. Selection: Tapping exercises toggles selection with visual feedback
6. Bottom bar: Count updates, Start navigates forward
</verification>

<success_criteria>
- TRAIN button navigates to ExerciseSelectScreen with correct zoneId
- ExerciseSelectScreen shows only exercises for that zone (e.g., biceps shows Barbell Curl, Hammer Curl, etc.)
- Search input filters exercises by name
- Multi-select works with visual confirmation (ember border on selected)
- Bottom bar shows selection count and Start Workout button
- Start Workout navigates to WorkoutSession placeholder
</success_criteria>

<output>
After completion, create `.planning/phases/04-workout-module/04-01-SUMMARY.md`
</output>

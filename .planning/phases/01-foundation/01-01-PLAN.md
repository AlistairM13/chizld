---
phase: 01-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/constants/colors.ts
  - src/constants/fonts.ts
  - src/constants/zones.ts
  - src/constants/xp.ts
  - src/types/index.ts
  - src/components/character/ZonePaths.ts
  - assets/images/characters/muscle-front.png
  - scripts/convert-svg.js
autonomous: true

must_haves:
  truths:
    - "Design system constants exist and export correct values matching PRD spec"
    - "TypeScript types exist for all domain entities"
    - "SVG has been converted to PNG at the correct asset path"
    - "Zone path placeholder data exists for Phase 2"
  artifacts:
    - path: "src/constants/colors.ts"
      provides: "Full PRD color palette"
      exports: ["colors"]
    - path: "src/constants/fonts.ts"
      provides: "Font family name mappings using @expo-google-fonts underscore convention"
      exports: ["fonts"]
    - path: "src/constants/zones.ts"
      provides: "8 zone configs with IDs, names, sides, positions"
      exports: ["zones", "ZONE_IDS"]
    - path: "src/constants/xp.ts"
      provides: "Level thresholds 1-10 with titles"
      exports: ["LEVEL_THRESHOLDS"]
    - path: "src/types/index.ts"
      provides: "Domain type definitions"
      exports: ["Zone", "Exercise", "WorkoutSession", "WorkoutSet", "ZoneStats", "XPHistory"]
    - path: "src/components/character/ZonePaths.ts"
      provides: "Zone bounding box placeholders for Phase 2"
      exports: ["ZoneId", "ZoneBounds", "zoneBounds"]
    - path: "assets/images/characters/muscle-front.png"
      provides: "High-res PNG of character figure for Skia rendering"
  key_links:
    - from: "src/constants/zones.ts"
      to: "src/types/index.ts"
      via: "imports ZoneId type"
      pattern: "import.*ZoneId.*from.*types"
    - from: "src/components/character/ZonePaths.ts"
      to: "src/types/index.ts"
      via: "imports ZoneId type"
      pattern: "import.*ZoneId.*from.*types"
---

<objective>
Create the file structure skeleton, design system constants, TypeScript domain types, and convert the SVG character asset to PNG.

Purpose: Every downstream plan and phase depends on these constants, types, and assets being in place. This is the foundation of the foundation.
Output: All src/ subdirectories created, constants files (colors, fonts, zones, xp), domain type definitions, ZonePaths.ts placeholder, and muscle-front.png asset.
</objective>

<execution_context>
@C:\Users\alist\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\alist\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-RESEARCH.md

Source references:
@prd.md (sections 4.1 Colors, 4.2 Typography, 3.2 Zone Configuration, 7 XP & Leveling)
@designs/muscle-front.svg (source SVG for conversion)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create directory structure, constants, and types</name>
  <files>
    src/constants/colors.ts
    src/constants/fonts.ts
    src/constants/zones.ts
    src/constants/xp.ts
    src/types/index.ts
    src/navigation/ (directory)
    src/screens/character/ (directory)
    src/screens/workout/ (directory)
    src/screens/sleep/ (directory)
    src/screens/diet/ (directory)
    src/components/character/ (directory)
    src/components/shared/ (directory)
    src/db/ (directory)
    src/services/ (directory)
    src/hooks/ (directory)
  </files>
  <action>
    1. Create all PRD-specified directories under src/. The existing src/constants/ can stay (theme.ts will be superseded by the new files). The existing src/hooks/ can stay. Do NOT delete src/app/ or src/components/ yet -- Plan 01-03 handles that cleanup.

    2. Create src/constants/colors.ts -- Export a `colors` object matching PRD section 4.1 EXACTLY:
       - bg: { primary: '#0A0A0F', card: '#141419', elevated: '#1C1C24' }
       - ember: { 100: '#FFF0E0', 300: '#FFB366', 500: '#FF8C1A', 700: '#CC6600', glow: 'rgba(255,140,26,0.4)' }
       - zone: { cold: '#2A2A3A', warm: 'rgba(255,140,26,0.25)' }
       - hud: { teal: 'rgba(26,92,92,0.15)', scanLine: 'rgba(255,255,255,0.02)' }
       - text: { primary: '#F0F0F5', secondary: '#8888A0', muted: '#555566', system: '#333344' }
       - success: '#4ADE80', warning: '#FBBF24', danger: '#EF4444'
       Use `as const` for type safety.

    3. Create src/constants/fonts.ts -- Export a `fonts` object with CORRECT @expo-google-fonts underscore convention names (NOT the PRD's hyphenated names which are WRONG):
       - display: 'ChakraPetch_700Bold'
       - displaySemiBold: 'ChakraPetch_600SemiBold'
       - displayRegular: 'ChakraPetch_400Regular'
       - heading: 'BarlowCondensed_600SemiBold'
       - label: 'BarlowCondensed_500Medium'
       - labelRegular: 'BarlowCondensed_400Regular'
       - mono: 'JetBrainsMono_700Bold'
       - monoLight: 'JetBrainsMono_400Regular'
       Use `as const` for type safety.

    4. Create src/constants/zones.ts -- Export `ZONE_IDS` array and `zones` config array/record for 8 zones from PRD section 3.2:
       Each zone has: id (ZoneId type), name (display string, uppercase), side ('left' | 'right'), position ('upper' | 'mid-upper' | 'mid-lower' | 'lower').
       Zones: traps (Left/Upper), biceps (Left/Mid-upper), forearms (Left/Mid-lower), tibialis (Left/Lower), neck (Right/Upper), shoulders (Right/Mid-upper), abs (Right/Mid-lower), quads (Right/Lower).
       Import ZoneId from types.

    5. Create src/constants/xp.ts -- Export `LEVEL_THRESHOLDS` array from PRD section 7:
       Level 1: 0 XP "Untrained", Level 2: 100 "Beginner", Level 3: 300 "Novice", Level 4: 600 "Intermediate", Level 5: 1000 "Dedicated", Level 6: 1500 "Advanced", Level 7: 2200 "Veteran", Level 8: 3000 "Elite", Level 9: 4000 "Master", Level 10: 5500 "Chizld".
       Each entry: { level: number, xpRequired: number, title: string }.

    6. Create src/types/index.ts -- Export TypeScript interfaces for ALL domain entities:
       - ZoneId: string literal union of 8 zone IDs
       - Zone: { id: ZoneId, name: string, side: 'left' | 'right', position: string }
       - Exercise: { id: string, name: string, primaryZone: ZoneId, secondaryZones?: string, equipment?: string, isCustom: boolean, createdAt: string }
       - WorkoutSession: { id: string, startedAt: string, endedAt?: string, notes?: string, photoId?: string, createdAt: string }
       - WorkoutSet: { id: string, sessionId: string, exerciseId: string, setNumber: number, weightKg?: number, reps?: number, rpe?: number, tempoEccentric?: number, tempoPauseBottom?: number, tempoConcentric?: number, tempoPauseTop?: number, completedAt?: string, createdAt: string }
       - ZoneStats: { zoneId: ZoneId, totalXp: number, level: number, currentStreak: number, longestStreak: number, lastTrainedAt?: string, personalRecords?: string, createdAt: string, updatedAt: string }
       - XPHistory: { id: string, zoneId: ZoneId, xpAmount: number, source: string, sourceId?: string, earnedAt: string }
       - LevelThreshold: { level: number, xpRequired: number, title: string }
  </action>
  <verify>
    Run: `npx tsc --noEmit src/types/index.ts src/constants/colors.ts src/constants/fonts.ts src/constants/zones.ts src/constants/xp.ts` -- no TypeScript errors.
    Verify directories exist: `ls src/navigation src/screens/character src/screens/workout src/screens/sleep src/screens/diet src/components/character src/components/shared src/db src/services src/hooks`
  </verify>
  <done>
    All 8 PRD-specified src/ subdirectories exist. colors.ts exports the full PRD color palette. fonts.ts exports correct @expo-google-fonts underscore-convention names (NOT PRD's wrong hyphenated names). zones.ts exports 8 zone configs. xp.ts exports 10 level thresholds. types/index.ts exports all domain interfaces. No TypeScript errors.
  </done>
</task>

<task type="auto">
  <name>Task 2: Convert SVG to PNG and create ZonePaths placeholder</name>
  <files>
    scripts/convert-svg.js
    assets/images/characters/muscle-front.png
    src/components/character/ZonePaths.ts
  </files>
  <action>
    1. Create scripts/convert-svg.js:
       - Use sharp to convert designs/muscle-front.svg to assets/images/characters/muscle-front.png
       - Resolution: 555x1116 (3x of 185x372 SVG viewBox) for crisp display
       - Create the output directory (assets/images/characters/) if it doesn't exist
       - sharp may need to be installed temporarily: run `npm install --no-save sharp` first
       - Script: `const sharp = require('sharp'); const path = require('path'); async function convert() { await sharp(path.join(__dirname, '..', 'designs', 'muscle-front.svg')).resize(555, 1116).png().toFile(path.join(__dirname, '..', 'assets', 'images', 'characters', 'muscle-front.png')); console.log('Done: muscle-front.png 555x1116'); } convert().catch(console.error);`

    2. Run the conversion: `node scripts/convert-svg.js`
       - Verify the PNG was created and is non-zero size
       - Optionally clean up sharp: `npm uninstall sharp` (since the PNG is now committed and the script only runs once)

    3. Create src/components/character/ZonePaths.ts:
       - Import ZoneId from src/types/index.ts
       - Export ZoneBounds interface: { x: number, y: number, width: number, height: number }
       - Export zoneBounds: Record<ZoneId, ZoneBounds> with approximate bounding boxes in SVG coordinate space (0 0 185 372):
         traps: { x: 60, y: 45, width: 65, height: 30 }
         biceps: { x: 15, y: 80, width: 35, height: 50 }
         forearms: { x: 8, y: 135, width: 30, height: 50 }
         tibialis: { x: 55, y: 280, width: 30, height: 55 }
         neck: { x: 80, y: 15, width: 25, height: 30 }
         shoulders: { x: 110, y: 45, width: 50, height: 35 }
         abs: { x: 70, y: 150, width: 45, height: 60 }
         quads: { x: 60, y: 210, width: 65, height: 70 }
       - Add a clear TODO comment: "Phase 2: Replace with actual path data from muscle-front.svg visual inspection"
       - These are placeholder estimates -- Phase 2 will refine them when BodyCanvas is implemented
  </action>
  <verify>
    Run: `ls -la assets/images/characters/muscle-front.png` -- file exists and is > 0 bytes.
    Run: `npx tsc --noEmit src/components/character/ZonePaths.ts` -- no TypeScript errors.
  </verify>
  <done>
    muscle-front.png exists at assets/images/characters/ at 555x1116 resolution. ZonePaths.ts exports ZoneId, ZoneBounds, and zoneBounds with placeholder bounding boxes for all 8 zones. The blocker noted in STATE.md ("muscle-front.png is missing from assets/images/characters/") is resolved.
  </done>
</task>

</tasks>

<verification>
- All src/ subdirectories from PRD spec exist
- Constants files compile without errors
- Types file exports all domain interfaces
- PNG asset exists at correct path
- ZonePaths.ts compiles and exports zone data
</verification>

<success_criteria>
1. `src/constants/colors.ts` exports colors object matching PRD section 4.1
2. `src/constants/fonts.ts` exports fonts object with CORRECT underscore-convention names
3. `src/constants/zones.ts` exports 8 zone configs
4. `src/constants/xp.ts` exports 10 level thresholds
5. `src/types/index.ts` exports Zone, Exercise, WorkoutSession, WorkoutSet, ZoneStats, XPHistory, LevelThreshold
6. `assets/images/characters/muscle-front.png` exists (555x1116)
7. `src/components/character/ZonePaths.ts` exports zoneBounds for 8 zones
8. All directories exist: navigation, screens/{character,workout,sleep,diet}, components/{character,shared}, db, services, hooks
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-01-SUMMARY.md`
</output>

---
phase: 04.1-workout-session-polish
plan: 03
type: execute
wave: 2
depends_on: ["04.1-01"]
files_modified:
  - src/components/workout/RestTimerOverlay.tsx
autonomous: true

must_haves:
  truths:
    - "Rest timer shows full black screen with only large centered countdown seconds"
    - "Countdown displays just the number (e.g. '45'), not '0:45' format"
    - "Below countdown: -15s, SKIP, +15s buttons in a row"
    - "When timer hits zero: shows REST COMPLETE, user taps to return"
    - "No Skia progress ring — pure black screen with text only"
  artifacts:
    - path: "src/components/workout/RestTimerOverlay.tsx"
      provides: "Simplified full-black rest timer overlay"
  key_links:
    - from: "src/components/workout/RestTimerOverlay.tsx"
      to: "src/hooks/useRestTimer.ts"
      via: "hook for timer state"
      pattern: "useRestTimer"
---

<objective>
Simplify RestTimerOverlay to a full-black screen with only a large countdown number, a button row (-15s / SKIP / +15s), and a "REST COMPLETE" state when timer reaches zero. Remove the Skia progress ring entirely. Do NOT add entering/exiting FadeIn/FadeOut animations here — Plan 04 owns the fade transition wrapper around this component in WorkoutSessionScreen.

Purpose: The rest view should create a stark visual break from the active set view — completely black with nothing but the countdown timer, creating a sense of rest and recovery.
Output: Simplified RestTimerOverlay.tsx
</objective>

<execution_context>
@C:\Users\alist\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\alist\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@src/components/workout/RestTimerOverlay.tsx
@src/hooks/useRestTimer.ts
@src/constants/colors.ts
@src/constants/fonts.ts
@.planning/phases/04.1-workout-session-polish/04.1-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Simplify RestTimerOverlay to full-black minimal timer</name>
  <files>src/components/workout/RestTimerOverlay.tsx</files>
  <action>
Completely rewrite the RestTimerOverlay component. Remove all Skia imports and the progress ring. The new design is purely text-based on a full black background.

**REMOVE:**
- All Skia imports (`Canvas`, `Path`, `Skia` from `@shopify/react-native-skia`)
- `createArcPath` function
- `RING_SIZE`, `STROKE_WIDTH`, `RADIUS`, `CENTER` constants
- The Canvas/Path ring rendering
- `Vibration` import (use only Haptics)
- Any Reanimated entering/exiting animation imports (FadeIn/FadeOut are NOT needed here — Plan 04 handles the wrapper animation)

**DO NOT ADD Reanimated FadeIn/FadeOut.** The parent (WorkoutSessionScreen, Plan 04) wraps this component in an Animated.View with FadeIn/FadeOut. Adding FadeIn inside this component would create a double-fade conflict.

**NEW INTERFACE — add `onDismiss` prop:**
```typescript
interface RestTimerOverlayProps {
  duration: number;
  onComplete: () => void;
  onSkip: () => void;
}
```

**STATE:**
- Keep using `useRestTimer(duration)` hook
- Add `useState<boolean>` for `isComplete` — set to true when timer reaches 0
- When timer reaches 0: set isComplete = true, trigger haptics, do NOT auto-call onComplete

**LAYOUT (when timer is running, isComplete = false):**

Wrap everything in a regular `View` with style `absoluteFillObject` (NO Animated.View, NO FadeIn — the parent handles fade transitions):

1. **Full black background:** `backgroundColor: '#000000'` (pure black, not colors.bg.primary)
2. **Centered countdown number:**
   - Just the seconds number, large: e.g., "45" not "0:45"
   - fonts.mono, fontSize: 120, colors.text.primary
   - If remaining >= 60, show just seconds portion anyway (the decision says "just '45', not '0:45'")
   - Actually, for remaining >= 60: show remaining seconds as-is (e.g., remaining=90 shows "90")
   - letterSpacing: 4
3. **Button row below countdown (marginTop: 48):**
   - Three buttons in a row with gap: 32
   - "-15s" button: border 1px colors.ember[700], paddingHorizontal 20, paddingVertical 10, fonts.mono fontSize 16, colors.ember[500]
   - "SKIP" button: same border style but text in fonts.display, fontSize 14, colors.text.muted, letterSpacing 2
   - "+15s" button: same as -15s
   - Keep existing handleAdjust and handleSkip logic

**LAYOUT (when timer is complete, isComplete = true):**

1. **Same full black background**
2. **Centered "REST COMPLETE" text:**
   - fonts.display, fontSize 28, colors.ember[500], letterSpacing 4
3. **Below: "TAP TO CONTINUE" text:**
   - fonts.monoLight, fontSize 12, colors.text.muted, letterSpacing 2, marginTop 16
4. **Wrap entire overlay in a Pressable** — on press when isComplete, call onComplete()

**Timer completion effect:**
Change the existing useEffect that monitors timer.isRunning and timer.remaining:
- When `!timer.isRunning && timer.remaining === 0 && duration > 0`:
  - Set `isComplete` to true
  - `Haptics.notificationAsync(Haptics.NotificationFeedbackType.Warning)`
  - Do NOT call onComplete() — user must tap

**handleSkip:** Still calls onSkip() directly (skipping doesn't require tap-to-continue)
  </action>
  <verify>File has no Skia imports. File has NO Reanimated FadeIn/FadeOut imports (parent owns transitions). File renders "REST COMPLETE" text. File uses pure black '#000000' background.</verify>
  <done>RestTimerOverlay shows a minimal full-black screen with large countdown seconds, -15s/SKIP/+15s buttons, and a tap-to-continue "REST COMPLETE" state</done>
</task>

</tasks>

<verification>
1. No Skia imports in RestTimerOverlay.tsx
2. No Reanimated FadeIn/FadeOut imports in RestTimerOverlay.tsx (Plan 04 owns fade transitions)
3. Background is pure black (#000000)
4. Countdown shows just the number (no colon format)
5. Three-button row: -15s, SKIP, +15s
6. Timer completion shows "REST COMPLETE" + "TAP TO CONTINUE"
</verification>

<success_criteria>
The rest timer overlay is a stark full-black screen with only a large countdown number, creating a visual break from the active set view. On completion, user taps to return (not auto-dismiss).
</success_criteria>

<output>
After completion, create `.planning/phases/04.1-workout-session-polish/04.1-03-SUMMARY.md`
</output>

---
phase: 02-character-overview
plan: 03
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - src/components/character/HudBar.tsx
  - src/components/character/TypewriterText.tsx
  - src/hooks/useUptimeCounter.ts
  - src/screens/character/CharacterScreen.tsx
autonomous: true

must_haves:
  truths:
    - "User sees CHIZLD branding top-left in Chakra Petch font"
    - "User sees cryptic system codes top-right in JetBrains Mono"
    - "User sees zone count and uptime in bottom bar"
    - "Text types in character-by-character on screen load"
    - "HUD bars have thin border lines (panel style)"
  artifacts:
    - path: "src/components/character/HudBar.tsx"
      provides: "Top and bottom HUD bar components"
      exports: ["HudBarTop", "HudBarBottom"]
    - path: "src/components/character/TypewriterText.tsx"
      provides: "Animated typewriter text effect"
      exports: ["TypewriterText"]
    - path: "src/hooks/useUptimeCounter.ts"
      provides: "Session uptime calculation and formatting"
      exports: ["useUptimeCounter"]
  key_links:
    - from: "src/components/character/TypewriterText.tsx"
      to: "useState + setInterval"
      via: "character reveal animation"
      pattern: "setInterval.*displayedChars"
    - from: "src/hooks/useUptimeCounter.ts"
      to: "useEffect + setInterval"
      via: "time tracking"
      pattern: "setInterval.*seconds"
    - from: "src/components/character/HudBar.tsx"
      to: "fonts.display"
      via: "CHIZLD branding"
      pattern: "fontFamily.*ChakraPetch"
---

<objective>
Create HUD bars (top and bottom) with typewriter text animation for the cyberpunk terminal boot aesthetic.

Purpose: Complete the UI frame of the character overview with branding, system status, and the signature terminal boot feel that sets the cyberpunk tone.

Output: Top bar with CHIZLD branding and system codes, bottom bar with zone count and uptime, all text animating in with typewriter effect on load.
</objective>

<execution_context>
@.claude/workflows/execute-plan.md
@.claude/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-character-overview/02-RESEARCH.md
@.planning/phases/02-character-overview/02-CONTEXT.md
@.planning/phases/02-character-overview/02-01-SUMMARY.md
@src/constants/colors.ts
@src/constants/fonts.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create TypewriterText and useUptimeCounter</name>
  <files>
    src/components/character/TypewriterText.tsx
    src/hooks/useUptimeCounter.ts
  </files>
  <action>
    Create `src/components/character/TypewriterText.tsx`:
    - React Native Text component with character reveal animation
    - Props:
      - `text: string` - full text to display
      - `delayMs?: number` - ms between characters (default: 30-50, Claude's discretion)
      - `style?: TextStyle` - pass-through for font styling
      - `onComplete?: () => void` - callback when animation finishes
    - Use `useState` for displayedChars count
    - Use `useEffect` with `setInterval` to increment displayedChars
    - Clear interval when text fully revealed
    - Display `text.slice(0, displayedChars)`
    - Optional: Show blinking cursor `_` while typing (remove after complete)
    - Export named component `TypewriterText`

    Reference 02-RESEARCH.md "Typewriter Text Animation" example.

    Create `src/hooks/useUptimeCounter.ts`:
    - Track session uptime since screen mounted
    - Return formatted string: "XXd XXh XXm" or "XXh XXm XXs"
    - Use `useState` for elapsed seconds
    - Use `useEffect` with `setInterval` (1000ms) to increment
    - Format function:
      - If >= 1 day: `${days}d ${hours}h`
      - If >= 1 hour: `${hours}h ${minutes}m`
      - Else: `${minutes}m ${seconds}s`
    - Export `useUptimeCounter` hook returning formatted string
  </action>
  <verify>
    - `npx tsc --noEmit` passes
    - TypewriterText exports correctly
    - useUptimeCounter exports correctly
  </verify>
  <done>
    TypewriterText animates text character-by-character. useUptimeCounter tracks and formats session duration.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create HudBarTop and HudBarBottom components</name>
  <files>
    src/components/character/HudBar.tsx
  </files>
  <action>
    Create `src/components/character/HudBar.tsx` with two exported components:

    **HudBarTop:**
    - Absolute positioned at top of screen
    - Left side: "CHIZLD" in `fonts.display` (Chakra Petch Bold), `colors.text.primary`
    - Right side: Cryptic system codes in `fonts.monoLight` (JetBrains Mono), `colors.text.muted`
      - Example text (Claude's discretion): "SYS::v0.1.0 // INIT::OK // MEM::42%"
      - Or simpler: "v0.1.0 // ACTIVE"
    - Thin border line below bar (`borderBottomWidth: 1`, `borderBottomColor: colors.zone.cold`)
    - Horizontal padding ~16px, height ~40-50px
    - Use `TypewriterText` for all text content
    - Pass different `delayMs` to stagger: branding starts first, system codes start slightly later

    **HudBarBottom:**
    - Absolute positioned at bottom of screen (above tab bar if visible)
    - Accept props: `activeZoneCount: number`, `totalZones: number` (default 8)
    - Content (all JetBrains Mono, `fonts.monoLight`):
      - Left: `ZONES: 8 / ACTIVE: ${activeCount}/8`
      - Right: `UPTIME: ${uptime}` (from useUptimeCounter)
    - Thin border line above bar (`borderTopWidth: 1`, `borderTopColor: colors.zone.cold`)
    - Use `TypewriterText` for animated reveal
    - Text color: `colors.text.secondary`

    Style both bars with:
    - `flexDirection: 'row'`
    - `justifyContent: 'space-between'`
    - `alignItems: 'center'`
    - Consistent padding and height

    Export: `HudBarTop`, `HudBarBottom`
  </action>
  <verify>
    - `npx tsc --noEmit` passes
    - Both components export correctly
    - Components use correct fonts from fonts.ts
  </verify>
  <done>
    HudBarTop displays CHIZLD branding and system codes. HudBarBottom displays zone count and uptime. Both have thin border lines and use TypewriterText animation.
  </done>
</task>

<task type="auto">
  <name>Task 3: Integrate HUD bars into CharacterScreen</name>
  <files>
    src/screens/character/CharacterScreen.tsx
  </files>
  <action>
    Update `src/screens/character/CharacterScreen.tsx`:

    - Import `HudBarTop`, `HudBarBottom` from '../../components/character/HudBar'
    - Import `useUptimeCounter` from '../../hooks/useUptimeCounter'
    - Call `useUptimeCounter()` to get formatted uptime string
    - Get `warmCount` from `useZoneStats()` (already called from plan 02-02)

    Add HUD bars to render tree:
    ```tsx
    <View style={styles.container}>
      <BodyCanvas zones={zoneStats} />

      {/* Zone cards from 02-02 */}
      {zoneStats.map(zone => (
        <ZoneCard key={zone.zoneId} zone={zone} position={...} />
      ))}

      {/* HUD bars */}
      <HudBarTop />
      <HudBarBottom activeZoneCount={warmCount} totalZones={8} />
    </View>
    ```

    - HUD bars use absolute positioning, so order in JSX doesn't matter for z-index
    - HudBarTop: `position: 'absolute', top: 0, left: 0, right: 0`
    - HudBarBottom: `position: 'absolute', bottom: 0, left: 0, right: 0`

    The typewriter effect should trigger on screen mount, creating the terminal boot feel.
  </action>
  <verify>
    - `npx tsc --noEmit` passes
    - Run app and verify:
      - Top bar shows "CHIZLD" left, system codes right
      - Bottom bar shows "ZONES: 8 / ACTIVE: X/8" left, "UPTIME: XXm XXs" right
      - All text animates in with typewriter effect on screen load
      - Border lines visible above/below bars
  </verify>
  <done>
    CharacterScreen displays complete HUD with top bar (branding, system codes) and bottom bar (zone count, uptime). Text types in with terminal boot animation.
  </done>
</task>

</tasks>

<verification>
After all tasks complete:
1. Run `npx tsc --noEmit` - should pass with no errors
2. Run app on device/emulator
3. Verify: Top bar visible with "CHIZLD" left (Chakra Petch Bold) and system text right (JetBrains Mono)
4. Verify: Bottom bar visible with zone count left and uptime right (both JetBrains Mono)
5. Verify: Thin border lines above bottom bar and below top bar
6. Verify: Text animates in character-by-character on screen load (typewriter effect)
7. Verify: Uptime counter increments over time
8. Verify: Active zone count shows 0/8 (no zones trained yet)
</verification>

<success_criteria>
- TypewriterText animates text reveal smoothly
- useUptimeCounter increments and formats correctly
- Top bar displays CHIZLD branding in Chakra Petch Bold
- Bottom bar displays zone stats and uptime in JetBrains Mono
- Border lines visible (thin, subtle)
- All HUD text types in on screen load
- TypeScript compiles without errors
</success_criteria>

<output>
After completion, create `.planning/phases/02-character-overview/02-03-SUMMARY.md`
</output>

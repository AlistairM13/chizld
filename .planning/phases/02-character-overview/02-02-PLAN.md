---
phase: 02-character-overview
plan: 02
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - src/hooks/useZoneStats.ts
  - src/components/character/ZoneCard.tsx
  - src/components/character/ConnectingLines.tsx
  - src/components/character/ZoneGlow.tsx
  - src/components/character/BodyCanvas.tsx
  - src/screens/character/CharacterScreen.tsx
autonomous: true

must_haves:
  truths:
    - "User sees 8 zone cards positioned symmetrically (4 left, 4 right)"
    - "Each zone card shows zone name, level number, and + photo slot"
    - "Dashed connecting lines run from each card toward character body"
    - "Warm zones (trained within 3 days) display ember-orange borders and pulsing glow"
    - "Cold zones display grey borders and muted text"
    - "Glow intensity fades based on days since training"
  artifacts:
    - path: "src/hooks/useZoneStats.ts"
      provides: "Hook fetching zone_stats with warm/cold/intensity calculation"
      exports: ["useZoneStats", "ZoneWithIntensity"]
    - path: "src/components/character/ZoneCard.tsx"
      provides: "Beveled zone card with warm/cold styling"
      exports: ["ZoneCard"]
    - path: "src/components/character/ConnectingLines.tsx"
      provides: "Dashed lines from cards to character body"
      exports: ["ConnectingLines"]
    - path: "src/components/character/ZoneGlow.tsx"
      provides: "Pulsing ember glow for warm zones on character body"
      exports: ["ZoneGlow"]
  key_links:
    - from: "src/hooks/useZoneStats.ts"
      to: "useSQLiteContext"
      via: "database query"
      pattern: "useSQLiteContext.*getAllAsync"
    - from: "src/components/character/ZoneCard.tsx"
      to: "useZoneStats"
      via: "warm/cold state"
      pattern: "isWarm|intensity"
    - from: "src/components/character/ConnectingLines.tsx"
      to: "DashPathEffect"
      via: "Skia dashed line"
      pattern: "DashPathEffect.*intervals"
    - from: "src/components/character/ZoneGlow.tsx"
      to: "useSharedValue"
      via: "pulsing animation"
      pattern: "withRepeat|pulseProgress"
---

<objective>
Create zone cards with warm/cold states, connecting lines, and body region glows driven by real zone_stats data.

Purpose: Complete the interactive zone display layer showing training recency. Users see which zones are "alive" (recently trained) vs "cold" (need attention).

Output: 8 zone cards around the character body, dashed connecting lines, and pulsing glow effects tied to SQLite data.
</objective>

<execution_context>
@.claude/workflows/execute-plan.md
@.claude/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-character-overview/02-RESEARCH.md
@.planning/phases/02-character-overview/02-CONTEXT.md
@.planning/phases/02-character-overview/02-01-SUMMARY.md
@src/constants/colors.ts
@src/constants/fonts.ts
@src/constants/zones.ts
@src/constants/layout.ts
@src/types/index.ts
@src/db/database.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create useZoneStats hook with warm/cold intensity calculation</name>
  <files>
    src/hooks/useZoneStats.ts
  </files>
  <action>
    Create `src/hooks/useZoneStats.ts`:

    Define interface `ZoneWithIntensity`:
    ```typescript
    export interface ZoneWithIntensity {
      zoneId: ZoneId;
      name: string;
      side: 'left' | 'right';
      position: string;
      level: number;
      totalXp: number;
      lastTrainedAt: string | null;
      isWarm: boolean;      // trained within 3 days
      intensity: number;    // 0.0 (cold) to 1.0 (just trained)
    }
    ```

    Implement `useZoneStats()` hook:
    - Use `useSQLiteContext()` to get database handle
    - Query `SELECT * FROM zone_stats` on mount
    - Join with `zones` constant to get name, side, position
    - Calculate warmth:
      - `THREE_DAYS_MS = 3 * 24 * 60 * 60 * 1000`
      - `elapsed = Date.now() - new Date(lastTrainedAt).getTime()`
      - `isWarm = elapsed < THREE_DAYS_MS`
      - `intensity = isWarm ? Math.max(0, 1 - elapsed / THREE_DAYS_MS) : 0`
    - Return array of `ZoneWithIntensity` objects
    - Also return `warmCount` (number of warm zones)

    Reference 02-RESEARCH.md Pattern 3 for warm zone detection.

    Export: `useZoneStats`, `ZoneWithIntensity`
  </action>
  <verify>
    - `npx tsc --noEmit` passes
    - Hook exports correctly typed interface
  </verify>
  <done>
    useZoneStats hook fetches zone_stats from SQLite, computes isWarm and intensity for each zone.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create ZoneCard, ConnectingLines, and ZoneGlow components</name>
  <files>
    src/components/character/ZoneCard.tsx
    src/components/character/ConnectingLines.tsx
    src/components/character/ZoneGlow.tsx
  </files>
  <action>
    Create `src/components/character/ZoneCard.tsx`:
    - React Native View component (NOT Skia - for text rendering)
    - Accept props: `zone: ZoneWithIntensity`, `position: { x: number, y: number }`
    - Style with absolute positioning based on position prop
    - Beveled/angular shape using:
      - Option A: CSS borderWidth with clip-path via borderRadius
      - Option B: Wrap with small Skia canvas for beveled border path
      - Prefer Option A for simplicity unless visually insufficient
    - Display:
      - Zone name (Barlow Condensed, `fonts.heading`)
      - Level number (JetBrains Mono, `fonts.mono`)
      - "+" photo slot placeholder (circular or square icon)
    - Warm styling: `colors.ember[500]` border and text, subtle shadow glow
    - Cold styling: `colors.zone.cold` (#2A2A3A) border, `colors.text.secondary` (#8888A0) text
    - Card dimensions: ~100w x 60h (Claude's discretion, must fit zone name + level)
    - Wrap in `Pressable` with no-op onPress (prepares for Phase 3 tapping)

    Create `src/components/character/ConnectingLines.tsx`:
    - Skia component rendering dashed lines from cards to body
    - Accept `zones: ZoneWithIntensity[]` and layout info
    - For each zone, draw line from card anchor to body anchor point (from ZONE_CARD_POSITIONS)
    - Use Skia `Path` with `DashPathEffect` (intervals: [4, 4] per CONTEXT.md)
    - Warm lines: `rgba(255,140,26,0.6)`
    - Cold lines: `rgba(136,136,160,0.4)`
    - Export as standalone component to add to BodyCanvas

    Create `src/components/character/ZoneGlow.tsx`:
    - Skia component for pulsing glow on character body region
    - Accept `zone: ZoneWithIntensity` and body anchor position
    - Only render if `zone.isWarm`
    - Use Reanimated `useSharedValue` + `withRepeat` + `withTiming` for breathing pulse
    - Duration ~2000ms, ease-in-out (organic heartbeat feel per CONTEXT.md)
    - Glow radius and opacity scaled by `intensity`
    - Use `RadialGradient` from ember.glow to transparent
    - Export named component `ZoneGlow`

    Reference 02-RESEARCH.md patterns for DashPathEffect, pulsing animation.
  </action>
  <verify>
    - `npx tsc --noEmit` passes
    - All 3 components export correctly
    - ZoneCard uses correct fonts from fonts.ts
  </verify>
  <done>
    ZoneCard renders beveled card with zone info and warm/cold styling. ConnectingLines draws dashed paths. ZoneGlow provides pulsing ember glow for warm zones.
  </done>
</task>

<task type="auto">
  <name>Task 3: Integrate zone components into CharacterScreen</name>
  <files>
    src/components/character/BodyCanvas.tsx
    src/screens/character/CharacterScreen.tsx
  </files>
  <action>
    Update `src/components/character/BodyCanvas.tsx`:
    - Add props: `zones: ZoneWithIntensity[]` (optional, for glows and lines)
    - Import ConnectingLines and ZoneGlow components
    - Add ConnectingLines to render tree (after character image, before scan line)
    - Add ZoneGlow for each warm zone (render glow at body anchor points)
    - Render order: HexGrid, ScanFrame, Character Image, PlatformGlow, ZoneGlows, ConnectingLines, ScanLine

    Update `src/screens/character/CharacterScreen.tsx`:
    - Import `useZoneStats` hook
    - Import `ZoneCard` component
    - Import `ZONE_CARD_POSITIONS` from layout.ts
    - Call `useZoneStats()` to get zone data
    - Pass zones to BodyCanvas: `<BodyCanvas zones={zoneStats} />`
    - Render ZoneCard components overlaid on canvas:
      ```tsx
      <View style={styles.container}>
        <BodyCanvas zones={zoneStats} />
        {zoneStats.map(zone => (
          <ZoneCard
            key={zone.zoneId}
            zone={zone}
            position={ZONE_CARD_POSITIONS[zone.zoneId]}
          />
        ))}
        {/* HUD bars will be added in plan 02-03 */}
      </View>
      ```
    - Cards render with absolute positioning AFTER BodyCanvas (z-order: on top)
  </action>
  <verify>
    - `npx tsc --noEmit` passes
    - CharacterScreen fetches zone stats from SQLite
    - Run app and verify:
      - 8 zone cards visible around character (4 left, 4 right)
      - Dashed connecting lines from each card to body
      - Cold zones have grey styling (all zones cold initially - no training data)
  </verify>
  <done>
    CharacterScreen displays 8 zone cards with connecting lines and warm/cold states driven by zone_stats table.
  </done>
</task>

</tasks>

<verification>
After all tasks complete:
1. Run `npx tsc --noEmit` - should pass with no errors
2. Run app on device/emulator
3. Verify: 8 zone cards visible - 4 left (TRAPS, BICEPS, FOREARMS, TIBIALIS), 4 right (NECK, SHOULDERS, ABS, QUADS)
4. Verify: Each card shows zone name (Barlow Condensed), level number (JetBrains Mono), and + slot
5. Verify: Dashed connecting lines from each card to body area
6. Verify: All zones appear "cold" (grey borders, muted text) since no training data exists
7. Optional manual test: Update zone_stats.last_trained_at in SQLite to test warm state:
   - Zone should show ember-orange border and text
   - Body region should show pulsing glow
   - Connecting line should be ember-orange
</verification>

<success_criteria>
- useZoneStats hook successfully queries SQLite zone_stats table
- 8 zone cards render in correct positions (symmetrical around body)
- Zone cards display correct fonts (Barlow Condensed for name, JetBrains Mono for level)
- Connecting lines render with dashed pattern
- Cold zone styling applied correctly (grey borders, muted text)
- TypeScript compiles without errors
</success_criteria>

<output>
After completion, create `.planning/phases/02-character-overview/02-02-SUMMARY.md`
</output>

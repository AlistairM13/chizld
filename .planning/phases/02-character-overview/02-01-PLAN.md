---
phase: 02-character-overview
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/character/BodyCanvas.tsx
  - src/components/character/HexGrid.tsx
  - src/components/character/ScanFrame.tsx
  - src/components/character/ScanLine.tsx
  - src/components/character/PlatformGlow.tsx
  - src/constants/layout.ts
autonomous: true

must_haves:
  truths:
    - "User sees anatomical figure centered on #0A0A0F background"
    - "User sees hex grid pattern visible at ~5% opacity behind figure"
    - "User sees teal scan frame bracket around the character"
    - "User sees subtle scan line sweeping vertically through frame"
    - "User sees ember glow platform under character feet"
  artifacts:
    - path: "src/components/character/BodyCanvas.tsx"
      provides: "Main Skia canvas composing all visual layers"
      exports: ["BodyCanvas"]
    - path: "src/components/character/HexGrid.tsx"
      provides: "Pre-computed hex grid path component"
      exports: ["HexGrid"]
    - path: "src/components/character/ScanFrame.tsx"
      provides: "Teal bracket frame around character"
      exports: ["ScanFrame"]
    - path: "src/components/character/ScanLine.tsx"
      provides: "Animated vertical scan line"
      exports: ["ScanLine"]
    - path: "src/components/character/PlatformGlow.tsx"
      provides: "Ember glow circle at character feet"
      exports: ["PlatformGlow"]
    - path: "src/constants/layout.ts"
      provides: "Screen dimensions, character sizing, card positions"
      exports: ["layout", "getCharacterLayout", "ZONE_CARD_POSITIONS"]
  key_links:
    - from: "src/components/character/BodyCanvas.tsx"
      to: "assets/images/characters/muscle-front.png"
      via: "useImage hook"
      pattern: "useImage.*muscle-front"
    - from: "src/components/character/HexGrid.tsx"
      to: "useMemo"
      via: "pre-computed path generation"
      pattern: "useMemo.*generateHexGrid"
    - from: "src/components/character/ScanLine.tsx"
      to: "useSharedValue"
      via: "Reanimated animation"
      pattern: "useSharedValue|withRepeat"
---

<objective>
Create the Skia BodyCanvas component with hex grid background, character image, teal scan frame, animated scan line, and platform glow.

Purpose: Establish the visual foundation layer for the character overview screen using Skia canvas. This is the base layer that zone cards and HUD bars will overlay.

Output: BodyCanvas component rendering the complete cyberpunk tactical display background with character figure.
</objective>

<execution_context>
@.claude/workflows/execute-plan.md
@.claude/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-character-overview/02-RESEARCH.md
@.planning/phases/02-character-overview/02-CONTEXT.md
@src/constants/colors.ts
@src/constants/zones.ts
@src/components/character/ZonePaths.ts
@assets/images/characters/muscle-front.png
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create layout constants and hex grid component</name>
  <files>
    src/constants/layout.ts
    src/components/character/HexGrid.tsx
  </files>
  <action>
    Create `src/constants/layout.ts`:
    - Export `getCharacterLayout(screenWidth, screenHeight)` function returning:
      - `characterWidth`, `characterHeight` (scaled to ~80% screen height, maintain 555:1116 aspect)
      - `characterX`, `characterY` (centered in screen)
      - `scanFramePadding` (spacing around character for frame)
    - Export `ZONE_CARD_POSITIONS` object mapping ZoneId to { x, y, anchorX, anchorY } positions:
      - Left zones (traps, biceps, forearms, tibialis): cards positioned left of center
      - Right zones (neck, shoulders, abs, quads): cards positioned right of center
      - Use proportional positioning (percentage of screen dimensions) for responsiveness
      - Include anchor points on the character body for connecting lines

    Create `src/components/character/HexGrid.tsx`:
    - Use Skia Path to draw hex grid pattern
    - Pre-compute hex positions using `useMemo` to avoid per-frame calculation
    - Hex size ~20px, line weight 0.5px (Claude's discretion per CONTEXT.md)
    - Color: `rgba(255,255,255,0.05)` for ~5% opacity
    - Accept `width` and `height` props for viewport sizing
    - Export named component `HexGrid`

    Reference research examples in 02-RESEARCH.md for hex geometry math.
  </action>
  <verify>
    - `npx tsc --noEmit` passes
    - layout.ts exports getCharacterLayout and ZONE_CARD_POSITIONS
    - HexGrid.tsx exports HexGrid component
  </verify>
  <done>
    Layout constants compute responsive character positioning. HexGrid renders hex pattern path.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create ScanFrame, ScanLine, PlatformGlow, and BodyCanvas</name>
  <files>
    src/components/character/ScanFrame.tsx
    src/components/character/ScanLine.tsx
    src/components/character/PlatformGlow.tsx
    src/components/character/BodyCanvas.tsx
  </files>
  <action>
    Create `src/components/character/ScanFrame.tsx`:
    - Skia component drawing teal bracket frame around character area
    - Use `colors.hud.teal` for color (rgba(26,92,92,0.15))
    - Draw L-shaped corners (not full rectangle) for bracket aesthetic
    - Accept layout props for positioning
    - Export named component `ScanFrame`

    Create `src/components/character/ScanLine.tsx`:
    - Animated horizontal line sweeping vertically through frame
    - Use Reanimated `useSharedValue` + `withRepeat` + `withTiming`
    - Duration ~4000ms (slow, ambient sweep per CONTEXT.md)
    - Color: `colors.hud.scanLine` (rgba(255,255,255,0.02))
    - Apply Skia `Blur` effect for soft appearance
    - Export named component `ScanLine`

    Create `src/components/character/PlatformGlow.tsx`:
    - Circular/elliptical ember glow at character feet position
    - Use Skia `Circle` or `Oval` with `RadialGradient`
    - Color gradient from `colors.ember.glow` center to transparent edge
    - Position at bottom-center of character
    - Export named component `PlatformGlow`

    Create `src/components/character/BodyCanvas.tsx`:
    - Main component composing all Skia elements
    - Use Skia `Canvas` with `StyleSheet.absoluteFill`
    - Use `useWindowDimensions` for responsive sizing
    - Use `useImage` to load muscle-front.png (path: require('../../../assets/images/characters/muscle-front.png'))
    - IMPORTANT: Check `if (!image) return null` to handle async loading
    - Use `getCharacterLayout` for positioning
    - Render order (back to front): HexGrid, ScanFrame, Character Image, PlatformGlow, ScanLine
    - Export named component `BodyCanvas`

    Reference 02-RESEARCH.md patterns for useImage, direct shared values, Blur effects.
  </action>
  <verify>
    - `npx tsc --noEmit` passes
    - All 4 components export correctly
    - BodyCanvas composes all sub-components
  </verify>
  <done>
    BodyCanvas renders complete Skia layer: hex grid background, character image centered, teal scan frame, platform glow, animated scan line.
  </done>
</task>

<task type="auto">
  <name>Task 3: Integrate BodyCanvas into CharacterScreen</name>
  <files>
    src/screens/character/CharacterScreen.tsx
  </files>
  <action>
    Update `src/screens/character/CharacterScreen.tsx`:
    - Import BodyCanvas from '../../components/character/BodyCanvas'
    - Replace current placeholder content with:
      ```tsx
      <View style={styles.container}>
        <BodyCanvas />
        {/* Zone cards and HUD bars will be added in plans 02-02 and 02-03 */}
      </View>
      ```
    - Keep container style with `flex: 1` and `backgroundColor: colors.bg.primary`
    - Remove the placeholder Text components (LOADING, INITIALIZING)

    The screen should now show the hex grid, character figure, scan frame, and animations.
  </action>
  <verify>
    - `npx tsc --noEmit` passes
    - CharacterScreen imports and renders BodyCanvas
    - Run app with `npx expo start` and verify visual output on device/emulator
  </verify>
  <done>
    CharacterScreen displays the Skia canvas with hex grid, character image, scan frame, platform glow, and animated scan line.
  </done>
</task>

</tasks>

<verification>
After all tasks complete:
1. Run `npx tsc --noEmit` - should pass with no errors
2. Run app on device/emulator
3. Verify: Dark #0A0A0F background with visible hex grid pattern
4. Verify: Character figure (muscle-front.png) centered in landscape view
5. Verify: Teal bracket frame visible around character
6. Verify: Scan line slowly sweeping vertically (subtle, not distracting)
7. Verify: Ember glow visible at character feet
</verification>

<success_criteria>
- BodyCanvas component renders without crashes
- Character image displays centered with correct aspect ratio
- Hex grid visible at low opacity in background
- Scan frame brackets visible in teal
- Scan line animation running smoothly (no jank)
- Platform glow visible at character feet
- TypeScript compiles without errors
</success_criteria>

<output>
After completion, create `.planning/phases/02-character-overview/02-01-SUMMARY.md`
</output>
